Что можно было бы сделать при наличии доп. времени:
* Класть все в базу (sqlite хотя бы). При старте сервиса инициализироваться оттуда;
* Добавить config.py и вынести туда захардкоженные переменные;
* Добавить *.env файл с параметрами для env контейнера и читать их в config через `os.environ.get('VAR')`;
* Добавить логирование;
* Сделать обработку ошибок ( на `on_message` первым делом :) );
* Добавить поддержку нескольких device_id. Пробрасывать на селектор на фронт;
* Написать README;